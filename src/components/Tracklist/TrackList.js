import React from 'react';
import styles from './TrackList.module.css';

const TrackList = () => {

    return (
    <div className={styles.TrackList}>
        {/* Add a map method that renders a ser of Track components  */}
    </div>
    );
};

export default TrackList;